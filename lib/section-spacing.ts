/** Horizontal padding shared by all page sections. */
export const sectionX = "px-6 sm:px-8 md:px-16";

/** Margin between sections — see `.page-section` in globals.css (3rem / 48px). */
export const sectionBlock = "page-section";
